# 故事紀要自動總結工具 - 領域詞彙表

## 核心概念

### 文檔 (Document)
使用者一次匯入的原始故事文字。可多個文檔疊加到同一個故事（如第1章、第2章分別匯入）。

### 批次 (Batch)
文檔被自動分割後的處理單元。每個批次是完整句子的集合，token 數不超過使用者設定的上限（預設 2000 tokens），且**絕不切斷完整句子**。批次是 AI 處理的最小單位。

### 條目 (Entry)
表格中的一行記錄（例如紀要表的一行），由一個或多個批次產出。

## 表格體系

### 紀要表 (Chronicle Sheet)
核心表，記錄故事事件的時間線。每條條目包含：
- 時間跨度 (Time Span)
- 地點 (Location)
- 紀要 (Chronicle Text) — 客觀敘述
- 概覽 (Summary) — 2~3句濃縮
- 重要對白 (Important Lines) — 關鍵對話摘錄
- 編碼索引 (Code Index) — 唯一標識

### 輔助表
- 角色表 (Character Sheet)
- 勢力表 (Faction Sheet)
- 物品表 (Item Sheet)
- 術式表 (Technique Sheet)

## 處理模式

### 連載模式 (Serial Mode)
故事還在生長中，逐文檔匯入、逐批處理，表格持續累積更新。

### 回顧模式 (Review Mode)
已完成的長篇故事一次性匯入分析，產出完整的紀要與時間線。

## 伏筆追蹤 (Foreshadowing Track)
每個伏筆記錄：文本、所屬批次、狀態（未解決/已解決）。當後續批次的內容滿足某伏筆的條件時，自動標記為已解決。

## 模板系統 (Template System)
使用者可自訂表格模板。每個模板定義一組表格（紀要表 + 輔助表），包含表格名稱、欄位定義、欄位類型、約束條件等。

### 內建模板
- **咒術迴戰模板** — 預設提供，包含角色/勢力/物品/術式表等特定欄位
- **通用模板** — 簡化的通用設定（紀要表 + 少量自訂欄位）

### 自訂模板
使用者可以：
- 從既有模板複製後修改
- 新增/刪除表格
- 調整表格欄位（名稱、型別、長度限制、枚舉值）
- 儲存為自訂模板並命名
- 匯出/匯入模板 JSON

### 模板粒度
- 整套模板（包含多個表格）
- 也允許單獨替換其中某個表格的定義

### 模板儲存
- 模板庫獨立存在於瀏覽器 LocalStorage
- 故事 JSON 匯出時嵌入當前的表格 schema，確保可獨立還原
